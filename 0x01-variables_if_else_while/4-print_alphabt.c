#include <stdio.h>
/**
 * main - program that prints the alphabet in lowercase, followed by a new line
 *
 * Return: Zero value
*/
   int main(avoid)
{
	char c ;
	for ( c = a ; c!=e ; c!=d ; c<=z )
	{c++}
        putchar ('c') ;
	putchar ('\n')
return 0;
}
